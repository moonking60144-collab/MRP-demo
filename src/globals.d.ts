// Side-effect CSS imports (e.g. `import './globals.css'` in app/layout.tsx).
// Next.js handles these at build time, but VS Code's TypeScript service
// reports ts(2882) without an explicit module declaration.
declare module '*.css';
